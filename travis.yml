sudo: required
language: generic
services:
  - docker

before_install:
  - docker build -t fahurleandro/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true fahurleandro/docker-react npm run test
